const express=require('express');const {authenticateJwt}=require('../middleware/auth');const {query}=require('../db');const router=express.Router();router.get('/me',authenticateJwt,async(req,res,next)=>{try{const result=await query('SELECT id, email, role, display_name AS "displayName", is_active AS "isActive" FROM users WHERE id = $1 LIMIT 1',[req.user.id]);const user=result&&result.rows[0];if(!user||!user.isActive)return res.status(401).json({error:'Unauthorized',message:'User account is inactive'});res.json({user});}catch(err){next(err);}});module.exports=router;
